" activate line numbers
set number

" define tab indent
set expandtab
set cindent
set tabstop=4
set shiftwidth=4
set nosmarttab

" don't try to be more compatible to older versions
set nocompatible

" show current cursor position at the bottom
set ruler

" show current mode at the bottom
set showmode

" define the colorsheme
colorscheme darkblue

filetype plugin on
set incsearch
syntax on
